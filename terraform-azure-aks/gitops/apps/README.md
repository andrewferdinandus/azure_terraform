# GitOps Applications

This folder contains the desired state for applications deployed through GitOps.

Planned environment folders:

- `dev/`
- `qa/`
- `prod/`

Each environment can contain Kubernetes manifests, Helm values, or Kustomize overlays.
